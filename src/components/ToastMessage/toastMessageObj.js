import { Flip } from 'react-toastify';

export const messageSettings = {
  position: 'top-center',
  autoClose: 3000,
  pauseOnHover: false,
  pauseOnFocusLoss: false,
  closeButton: false,
  transition: Flip,
};
